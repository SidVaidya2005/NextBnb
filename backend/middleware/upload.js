// TODO: configure multer with Cloudinary storage once cloudinaryService is wired.
// Skeleton only — no functional middleware yet.

// const multer = require('multer');
// const { CloudinaryStorage } = require('multer-storage-cloudinary');
// const cloudinary = require('../services/cloudinaryService');

function upload() {
  throw new Error('upload middleware not implemented');
}

module.exports = upload;
