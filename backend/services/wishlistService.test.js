describe("wishlistService", () => {
  it.todo("findForUser returns saved listings");
  it.todo("addListing is idempotent for the same listing");
  it.todo("removeListing removes only the targeted listing");
  it.todo("clear empties the wishlist");
});
