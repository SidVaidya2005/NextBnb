# Used only by the Playwright e2e run (`vite --mode e2e`). Mode-specific env
# files outrank .env, so this points the app at the e2e backend on :8090
# instead of the local dev backend on :8080. Safe to commit (no secrets).
VITE_API_BASE_URL=http://localhost:8090
